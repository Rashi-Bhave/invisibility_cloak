To see the magic, run the code with the following command:
python main.py

Once the camera opens, wait for a few seconds so that the background can be captured. 
Then hold a red cloth in front the camera. 
